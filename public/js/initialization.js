const contentAuthUser = document.querySelector("#content-auth-user");
const wrapperMessage = document.querySelector(".container-message");
const wrapperMessageTasksEmpty = document.querySelector(".container-message-tasks-empty");

//Globals
let globalData = {
	user: null,
};
